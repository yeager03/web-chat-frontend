import { FC, ReactElement } from "react";

// components
import BaseRequests from "../components/Requests";

const Requests: FC = (): ReactElement => {
	return <BaseRequests />;
};

export default Requests;
